<resources>
    <!-- Base application theme. -->
    <style name="Theme.PepsaDispatch" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.App.SplashTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimationDuration">900</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon_wrapper</item>
        <item name="android:windowSplashScreenBrandingImage">@drawable/branding_image</item>
        <item name="postSplashScreenTheme">@style/Theme.PepsaDispatch</item>
    </style>

    <style name="footer" parent="Theme.PepsaDispatch">
        <item name="fontFamily">@font/inter</item>
        <item name="android:textColor">@color/white</item>
    </style>
    
    <style name="dialog_header" parent="Theme.PepsaDispatch">
        <item name="fontFamily">@font/poppins_bold</item>
        <item name="android:textSize">@dimen/dimen_12sp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="title_case" parent="Theme.PepsaDispatch">
        <item name="fontFamily">@font/open_sans_extrabold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/dimen_30sp</item>
    </style>

    <style name="btn_style" parent="Theme.PepsaDispatch">
        <item name="fontFamily">@font/open_sans_bold</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/background_btn</item>
        <item name="android:text">@string/continue_txt</item>
        <item name="android:fontFamily">@font/open_sans_bold</item>
    </style>

    <dimen name="dimen_20sp">20sp</dimen>
    <dimen name="dimen_30sp">30sp</dimen>
</resources>